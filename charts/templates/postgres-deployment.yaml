apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres-container
        image: postgres:latest
        env:
        - name: POSTGRES_DB
          value: students
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          value: Aniket@12
        ports:
        - containerPort: 5432
